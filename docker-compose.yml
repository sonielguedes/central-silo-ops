services:
  silo-central:
    build: .
    container_name: silo-central-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"