# P0.1 - Validacao de Cadastros Server-Side

Data: 2026-06-06
Tenant validado: `silo-ops-001`
Marcador criado: `TESTE PERSISTENCIA`

## Escopo

Validacao dos modulos migrados para `CadastroStorage` + `/api/cadastro/*`.
O modulo `/operadores` ja estava aprovado e ficou fora desta rodada.

## Resultado Executivo

Todos os modulos abaixo criaram registro via API server-side, gravaram em JSON e continuaram disponiveis apos restart local do processo Next.js.
O restart Docker da VPS nao foi executado neste ambiente porque o comando `docker` nao esta disponivel.

## Evidencias Tecnicas

- `GET/POST /api/cadastro/*` usando header `X-Silo-Tenant: silo-ops-001`.
- Arquivos gravados em `E:\app\data\silo-ops-001\cadastro-*.json`, equivalente local do storage de producao `/app/data`.
- `grep` local encontrou 18 ocorrencias de `TESTE PERSISTENCIA` nos JSONs.
- Restart validado localmente: parar/subir `npm run start -- -p 3000` e reler os endpoints.
- Docker VPS pendente: `docker restart silo-piloto-web && sleep 10`.

## Matriz de Validacao

| Modulo | Arquivo JSON | Criou | Apareceu em anonimo | Persistiu apos restart | Status | Pendencia |
|---|---|---|---|---|---|---|
| `/frota` | `E:\app\data\silo-ops-001\cadastro-equipamentos.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/fazendas-talhoes` | `E:\app\data\silo-ops-001\cadastro-fazendas.json` + `cadastro-talhoes.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar UI autenticada e Docker na VPS |
| `/paradas` | `E:\app\data\silo-ops-001\cadastro-paradas.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/tipos` | `E:\app\data\silo-ops-001\cadastro-tipos.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/modelos` | `E:\app\data\silo-ops-001\cadastro-modelos.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/grupos` | `E:\app\data\silo-ops-001\cadastro-grupos.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/perfis` | `E:\app\data\silo-ops-001\cadastro-perfis.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/implementos` | `E:\app\data\silo-ops-001\cadastro-implementos.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |
| `/frota/estados-operacionais` | `E:\app\data\silo-ops-001\cadastro-estados.json` | Sim | Sim via GET sem sessao | Sim no restart local | APROVADO LOCAL | Validar `docker restart silo-piloto-web` na VPS |

## Comandos de Validacao Executados

```bash
npm run type-check
npm run build
```

```powershell
POST http://localhost:3000/api/cadastro/{entity}
GET  http://localhost:3000/api/cadastro/{entity}
Get-ChildItem E:\app\data -Recurse -Filter cadastro-*.json
Select-String -Path E:\app\data\**\cadastro-*.json -Pattern "TESTE PERSISTENCIA"
```

## Conclusao

A camada `CadastroStorage` + `/api/cadastro` esta funcional para os modulos migrados no ambiente local/producao-like.
Nao ha evidencia de persistencia apenas em `localStorage` nestes modulos.
Para fechar como APROVADO PRODUCAO, falta executar a mesma checagem na VPS apos `docker restart silo-piloto-web`.
